#function to ease tasks involving rolling window

# i= number of iteration (length of the window)  
fun="sqrt" # A character variable including the name of the function that you created or that you want to use for the rolling window
RW=function(i=100,ini=0,fin=100){
  initial=ini
  final=fin
  
for(i in initial:final){

} 
}
